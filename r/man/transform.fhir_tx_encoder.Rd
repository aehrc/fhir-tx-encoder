% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encoder.R
\name{transform.fhir_tx_encoder}
\alias{transform.fhir_tx_encoder}
\title{Encode a matrix of codes}
\usage{
\method{transform}{fhir_tx_encoder}(`_data`, X, ...)
}
\arguments{
\item{_data}{An object of class `fhir_tx_encoder`.}

\item{X}{A two-dimensional character matrix of codes, with one row per
observation and one column per code variable.}

\item{...}{Unused, present for compatibility with the `transform()` generic.}
}
\value{
A `Matrix::dgCMatrix` with `nrow(X)` rows and
  `ncol(X) * length(_data$feature_names)` columns.
}
\description{
Looks up the encoding of every code in `X`. The columns of `X` are encoded
independently and bound side by side, so a two-column input over an encoder
with `f` features produces `2 * f` columns.
}
\examples{
\dontrun{
encoder <- fhir_tx_encoder(
  scope = "http://snomed.info/sct?fhir_vs=ecl/(\%3E\%3E\%20363346000)"
)
transform(encoder, matrix(c("399981008", "363346000"), ncol = 1))
}
}
